function Form({ theme, language }) {
  return (
    <form action="" className="w-3/4 mx-auto">
      <label className="font-semibold">Deneme</label>
      <input
        type="text"
        className={`w-full border border-${theme}-border rounded p-2`}
      />
      <label className="font-semibold">Deneme</label>
      <input
        type="text"
        className={`w-full border border-${theme}-border rounded p-2`}
      />
      <label className="font-semibold">Deneme</label>
      <input
        type="text"
        className={`w-full border border-${theme}-border rounded p-2`}
      />
      <label className="font-semibold">Deneme</label>
      <input
        type="text"
        className={`w-full border border-${theme}-border rounded p-2`}
      />
      <label className="font-semibold">Deneme</label>
      <input
        type="text"
        className={`w-full border border-${theme}-border rounded p-2`}
      />
    </form>
  );
}

export default Form;
